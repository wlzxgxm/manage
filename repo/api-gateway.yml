server:
  port: 7999

zuul:
  routes:
    user-service:
      path: /u/**
      serviceId: user-service
    department-service:
      path: /d/**
      serviceId: department-service

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
